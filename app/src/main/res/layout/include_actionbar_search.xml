<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
        xmlns:android="http://schemas.android.com/apk/res/android"
        android:id="@+id/toolBar"
        android:orientation="horizontal"
        android:layout_width="match_parent"
        android:layout_height="@dimen/actionbar_height"
        android:focusable="true"
        android:focusableInTouchMode="true"
        android:background="@color/colorPrimary">
    <ImageView
        android:id="@+id/iv_mine"
        android:layout_width="@dimen/actionbar_height"
        android:layout_height="@dimen/actionbar_height"
        android:src="@mipmap/ic_nav_back"
        android:onClick="onClickBack"
        android:scaleType="centerInside"
        />
    <EditText
        android:id="@+id/et_search"
        android:layout_width="0dp"
        android:layout_height="30dp"
        android:layout_gravity="center_vertical"
        android:layout_marginRight="10dp"
        android:layout_weight="1"
        android:background="@drawable/circular_searchbar"
        android:drawablePadding="5dp"
        android:drawableLeft="@mipmap/ic_search_01"
        android:hint="@string/please_input_you_want_search"
        android:textSize="@dimen/text_size_caption_material"
        android:paddingLeft="10dp"
        android:paddingRight="10dp"/>
    <TextView
        android:id="@+id/tv_action_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:visibility="gone"
        android:textColor="@color/txt_white"
        android:textSize="@dimen/text_size_title_material"
        android:text="@string/app_name"/>
    </LinearLayout>
